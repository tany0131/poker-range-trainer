# poker-range-trainer

6-max テキサスホールデムのプリフロップ・レンジを覚えるトレーナー。ビルド不要の静的サイト。

配布先 = GitHub Pages https://tany0131.github.io/poker-range-trainer/
(push で Actions が verify → build → verify-bundle → deploy)。
Artifact 版は移行期間だけ併存 (URL は会話ログにある)。

## 出荷手順 (必ずこの順)

```
node tools/verify.mjs        # ソースを DOM stub 上で実行して検証
node tools/build.mjs         # 単一 HTML に畳む → dist/
node tools/verify-bundle.mjs # 畳んだ後のバンドルを実行して再検証
```

**`dist/` はコミットしない** (.gitignore 済み)。Actions が push のたびにその場で畳んで配る。
手元で 3 つ通してから push する — CI で落ちるのは、通していないのと同じ。

`verify-bundle.mjs` を省略しないこと。ソースが通ってもインライン化で順序が壊れる事故は起きる。

副次的に、Artifact 版を更新するときは**同じ file_path で再公開**すれば URL は変わらない
(成績 = localStorage が保たれる)。別パスで公開すると Artifact 版に入っている成績が失われる。
移行そのものは成績カードの「成績を書き出す / 読み込む」で行う (別オリジンなので自動では移らない)。

`js/equity.js` (対ランダム勝率) は `node tools/gen-equity.mjs`、`js/matchups.js` (169×169 対戦マトリクス) は
`node tools/gen-matchups.mjs`、`js/hu-ranges.js` (ヘッズアップの押し引き) は `node tools/gen-hu.mjs`
の生成物 — どれも手で編集しない。seed 固定モンテカルロなので再生成しても同じ値。
verify.mjs が既知の解析値 (AA=85.2%、AAvsKK≈82% 等) と照合し、さらに両者を突き合わせるクロスチェック
(equityVsRange(全クラス) = EQUITY_VS_RANDOM) で系統誤差を検出する。js/gto.js のナッシュソルバーは
搾取可能性 < 0.02bb を verify が毎回検査する (これが「均衡である」ことの保証)。`js/hu-ranges.js` も
同じ考え方で、焼いた頻度と 0.5 の丸めがその場のソルバーと一致することを verify が突き合わせる。

## 触る前に知っておくこと

### レンジの正本は `js/spots.js`

`RFI_SPECS` と `VS_RFI_SPECS` がこのアプリの唯一の正本。ここを変えたら `tools/verify.mjs` が
レンジの帯・不変条件を検算する。**検算を通さずにレンジを触らない** — 間違ったレンジで反復練習
させるのは、このアプリで最悪の失敗。

### ES modules を使わない

`file://` で直接開けることを維持している (ES modules は CORS で弾かれる)。
`js/*.js` はクラシックスクリプトで、グローバルスコープを共有する。`index.html` の
`<script src>` の**並び順が依存順**で、これが唯一の正本 — `tools/build.mjs` も
`tools/verify.mjs` も同じ正規表現でここから読む。ファイルを足したら index.html に
1 行足すだけでよい (ツール側に第二の一覧は無い)。

`ui-*.js` は `ui-core.js` (el マップ・色とラベル・SVG・共通グリッド) に依存するので、
ui 系の中では `ui-core.js` が先頭。

### PWA まわり (index.html の script タグの約束)

`manifest.webmanifest` / `sw.js` / `icons/` が PWA 一式。`sw.js` の `__BUILD_ID__` は
`build.mjs` が畳んだ HTML の sha に置き換える (置き換え忘れると直しても古い版が出続けるので、
verify-bundle がプレースホルダの残りを検査する)。

body 末尾の Service Worker 登録は **`<script data-sw>`** で書く。build.mjs と verify-bundle は
「**属性なしの script タグ = アプリ本体のバンドル**」で拾うので、属性を外すと本体と取り違える。
同じ理由で、**HTML のコメントや本文に素の script タグ名を書かない** (文字列ごと拾われて
バンドルが壊れる。実際に踏んだ)。build.mjs が個数を数えて止める。

アイコンの PNG は `node tools/gen-icons.mjs` (macOS 専用: qlmanage + sips) の生成物。
`icons/icon.svg` が正本で、PNG は焼いてコミットする (CI は焼かない)。

### ヘッズアップは「浅い押し引き」だけ

`js/spots-hu.js` が扱うのは 8 / 10 / 15bb の オールイン/フォールド だけで、答えの正本は
`js/gto.js` のソルバー (それを焼いたのが `js/hu-ranges.js`)。**深いヘッズアップ (100bb の
オープン / ディフェンス) は意図的な欠番** — 検算できる出どころを持っていないので、足すと
「正しいか分からないレンジで反復練習させる」ことになる。足したくなったら、まず出どころと
検算方法を決めること。FAQ と解説にもそう書いてあるので、実装だけ先に変えない。

`ALL_DRILLS` / `DRILL_BY_KEY` / `defaultModeFor` は spots.js ではなく **spots-hu.js の末尾**にある
(ドリルを定義する最後のファイルなので、そこでないとヘッズアップぶんを含められない)。
ドリルの種類を足すときは、読み込み順で索引より前に置く。

### レンジまわりの罠 (どれも verify.mjs がアサートしている)

- **SB は BTN の上位互換ではない。** `UTG ⊂ HJ ⊂ CO ⊂ BTN` は成り立つが、SB では 7 ハンド
  (Q3s, J5s, J4s, 53s, K8o, T8o, 98o) が**消える**。「後ろの席ほど広い」と単純化して
  一般化しないこと。UI でも赤く見せている。
- **BB vs SB は意図的な欠番。** 出典チャートの SB はリンプ混合戦略 (レイズは 24% だけ) が前提
  だが、本アプリの SB はレイズオンリー 41% で教えている。前提が食い違うので入れない。
  足したくなったら、まず SB のリンプをどう扱うかを決めること。
- **混合戦略は頻度の高い方に丸めてある。** ソルバーが「60% コール / 40% 3bet」とする手も、
  1 ハンド 1 答にするためどちらかに寄せている。そのぶん本来より数ポイント堅い。
- **`foldBaseline` は「素の出題をフォールド連打したときの正解率」。** 実アプリには復習キューが
  あるので、実際にフォールド連打すると**基準より下がる** (ミスした手が返ってくるため)。
  UI の文言はこの前提で書いてある。「基準 = フォールド連打で取れる点数」と単純に書き換えないこと。

### 試験モードは「モード」ではない

`js/exam.js` の試験モードは `MODES` (練習の切り替えボタン) ではなく、下に並ぶカードの 1 つ。
出題も採点も練習の流れから独立していて、成績に書くのは 20 問走り切った最後の 1 回だけ
(中断したぶんは記録しない)。時計は `setTimeout` の連鎖で、答えた瞬間と中止した瞬間に必ず止める。
verify は実時間を待たない偽の時計 (`runTimers`) で 5 秒の時間切れまで通している。

**「境界ハンド」と「境界線上の手」は別物。** `BOUNDARY_HANDS` = ポジションで答えが変わる 54 ハンド
(RFI 限定・境界特訓モードが使う)。`edgeHandsOf(drill)` = 1 スポットの中で隣のマスと答えが変わる手
(3ベットのスポットにもある・穴埋めテストと試験が使う)。用語集の「境界ハンド」がこの区別を書いている。

時間切れは `TIMEOUT_ACTION` という擬似 action として記録する。出題数・ハンド別成績・復習キュー・
ミス履歴には入るが、**弱点分析 (ミスの向き) には入れない** — 何も選んでいないので「強すぎ / 弱すぎ」
の向きが無い。ここを数えると傾向の分析が嘘になる。

### 検証の作り方

ブラウザ (Playwright / chrome-devtools) は他プロセスがプロファイルを専有していて使えないことが
多い。そのため `tools/verify.mjs` は **DOM を stub して Node の vm 上でアプリ本体を実際に実行** する。
UI の描画パス (ボタンの色付け、バナー) までここで叩いている。
**UI を変えたら verify.mjs にも通す** — ロジックだけ緑でも意味がない。

## 設計メモ

- `DRILLS` = 19 スポット (RFI 5 + vs RFI 14)。これにサイズ 19 とヘッズアップ 6 を足した
  `ALL_DRILLS` = 44 が索引で、弱点分析が回るのは `CATEGORY_DRILLS` (= サイズ以外 = カードを配るもの)。
  RFI も 3bet もヘッズアップも同じ「ドリル」の形にそろえてある。
  出題・採点・成績・狙い撃ちは全部このキー単位。
- 出題は **combos 重み付き** (AKo は AKs の 3 倍出る)。均等に出すとペアばかり来て練習にならない。
  例外は境界モードで、ここだけ一様に引く (全境界ハンドを均等に潰したいため)。
- 境界ハンド = ポジションで答えが変わる 54 ハンド。ここだけが「知識が要る」領域。
- state は破壊的更新をしない。`recordAnswer` は常に新しい state を返す。
